import { useState } from "react";
import TeamButton from "./TeamButton";

function TeamChoiceBar(){
    const [formData, setFormData] = useState(null);

    return (
        <div></div>
    )

}

export default TeamChoiceBar